# Runtime dependencies for the DEPLOYED Streamlit app.
# Keep this list tight - Streamlit Community Cloud build time is
# proportional to install size, and heavy deps like pyspark cause cold
# starts and occasional build timeouts.
#
# For the full local pipeline (PySpark transform, S3 upload, MLflow
# tracking, PDF generation), see requirements-pipeline.txt.

streamlit==1.39.0
pandas==2.2.3
numpy==1.26.4
scikit-learn==1.5.2
joblib==1.4.2
plotly==5.24.1
requests==2.32.3
pyarrow==17.0.0
holidays==0.59
python-dateutil==2.9.0.post0
